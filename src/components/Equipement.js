import React from "react";

function Equipement({ equipement }) {
    //ici texte est l'element sur lequel on est sur chaque itération
    equipement.map((texte,index)=> {})
  return <p> {equipement} </p>  

};


export default Equipement